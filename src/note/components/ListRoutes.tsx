import { List } from '@material-ui/core'
import React, { FunctionComponent } from 'react'
import ListItemAuth from './ListItemAuth'

const ListRoutes: FunctionComponent = () => {
  return (
    <List>
      <ListItemAuth />
    </List>
  )
}

export default ListRoutes
